# 2DCalibrationInterpolation
Get interpolated Z positions

Python: Generate A3200-Ready 2D calibration file (relies on scipy)
C++: Generate interpolation grid for command line sampling (relies on GNU GSL)
C#: Generate interpolation grid for runtime sampling (relies on ALGLIB)
